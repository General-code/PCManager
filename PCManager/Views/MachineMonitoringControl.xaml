<!--<UserControl x:Class="PCManager.Views.MachineMonitoringControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:scottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             xmlns:local="clr-namespace:PCManager.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Button Content="◀ Back to Map"
                VerticalAlignment="Top" HorizontalAlignment="Left"
                Margin="5" Padding="10,5" Panel.ZIndex="1"
                Command="{Binding DataContext.GoBackCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>

        <DataGrid Name="WeldingGrid" Grid.Column="0"
                  ItemsSource="{Binding WeldingHistory}"
                  AutoGeneratingColumn="WeldingGrid_AutoGeneratingColumn">
        </DataGrid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <scottPlot:WpfPlot Grid.Row="0" Name="BasicPlot"/>
            <scottPlot:WpfPlot Grid.Row="1" Name="NextPlot"/>
        </Grid>
    </Grid>
</UserControl>-->
<UserControl x:Class="PCManager.Views.MachineMonitoringControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF">

    <UserControl.Resources>
        <Style x:Key="BackButton" TargetType="Button">
            <Setter Property="Background" Value="DodgerBlue"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontFamily" Value="Arial"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="30,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left: Back Button + DataGrid -->
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button
                    Grid.Row="0"
                    Style="{StaticResource BackButton}"
                    Command="{Binding BackCommand}"
                    HorizontalAlignment="Left"
                    Margin="10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="◀ Back"/>
                </StackPanel>
            </Button>

            <DataGrid Grid.Row="1"
                      x:Name="WeldingGrid"
                      Margin="10"
                      AutoGenerateColumns="True"
                      AutoGeneratingColumn="WeldingGrid_AutoGeneratingColumn"
                      IsReadOnly="True"
                      AlternationCount="2"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Stretch"/>
        </Grid>

        <!-- Right: Two Charts -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ScottPlot:WpfPlot x:Name="BasicPlot" Grid.Row="0" Margin="10"/>
            <ScottPlot:WpfPlot x:Name="NextPlot" Grid.Row="1" Margin="10"/>
        </Grid>
    </Grid>
</UserControl>